# M4: Dashboard 可视化 + Agent 联动 Goal

将权限网关(cockpit-api)与 AI Agent(OpenClacky)打通，让用户通过自然语言查询业务数据并严格遵守权限边界。主界面为 OpenClacky Web UI，cockpit-api 纯后端，Auth Proxy 注入用户身份，自定义 cockpit_gateway Tool 替代 Skill/curl。

## Safety Boundary

Generated Epic documents are a contract for task generation. Task briefs must stay within this goal and the contract acceptance items.